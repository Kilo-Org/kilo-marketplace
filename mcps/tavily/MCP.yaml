id: tavily
name: Tavily
description: Connects AI assistants to Tavily's real-time web search and intelligent page extraction tools, with advanced
  filtering and domain controls for research and agent workflows.
author: tavily-ai
url: https://github.com/tavily-ai/tavily-mcp
category: search
prerequisites:
  - Tavily account
content:
  - name: NPX
    prerequisites:
      - Node.js
    content: |
      {
        "command": "npx",
        "args": ["-y", "tavily-mcp@latest"],
        "env": {
          "TAVILY_API_KEY": "{{TAVILY_API_KEY}}"
        }
      }
  - name: Docker
    prerequisites:
      - Docker
    content: |
      {
        "command": "docker",
        "args": [
          "run",
          "-i",
          "--rm",
          "-e",
          "TAVILY_API_KEY",
          "mcp/tavily"
        ],
        "env": {
          "TAVILY_API_KEY": "{{TAVILY_API_KEY}}"
        }
      }
parameters:
  - name: Tavily API Key
    key: TAVILY_API_KEY
    placeholder: your-tavily-apikey-here
